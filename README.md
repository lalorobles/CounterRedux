# CounterRedux
App developed in React Native with Expo CLI.

The app show a counter with 4 buttons to increment and  decrement by 1 and by 10, also has a button to reset the counter. For the Counter value the app use Redux to manage the state of value and the buttons was developed as componentand so they can recive props to be a configurable button.
